# Generated by groundwork — do not edit manually.
# Resource type: {{ .Type }}

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

resource "{{ .Type }}" "main" {
  # TODO: configure required attributes for {{ .Type }}
  tags = var.tags
}

variable "tags" {
  description = "Tags to apply to resources"
  type        = map(string)
  default     = {}
}
